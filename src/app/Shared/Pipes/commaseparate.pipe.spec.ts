import { CommaseparatePipe } from './commaseparate.pipe';

describe('CommaseparatePipe', () => {
  it('create an instance', () => {
    const pipe = new CommaseparatePipe();
    expect(pipe).toBeTruthy();
  });
});
